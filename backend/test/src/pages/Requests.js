import './Requests.css';

export default function Requests() {
  return (
    <div className="requests-container">
      <h2>Swap Requests</h2>
      <p>Tabs: Pending, Accepted, Rejected</p>
      <p>Shows request status with action buttons (Accept/Reject)</p>
    </div>
  );
}
